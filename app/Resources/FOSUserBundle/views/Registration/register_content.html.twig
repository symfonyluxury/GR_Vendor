<form class="" action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
    {{ form_widget(form) }}
    <div class="form-actions">
     <button type="submit" class="btn btn-large btn-success" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" >注册</button>
    </div>
</form>
