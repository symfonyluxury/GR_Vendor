<div class="form-border connection">
    <form class="form-stacked form-registration" action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
        {# include "GaorenVendorsBundle:Fragment:alert-message.html.twig" #}
        {{ form_widget(form) }}
        <div class="form-actions">
        <input type="submit" class="btn btn-large btn-success btn-long" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" />
        {% if(not is_granted('IS_AUTHENTICATED_REMEMBERED')) %}
            <a class="pull-right" href="{{ path('fos_user_security_login')}}">→ {{ 'security.login.submit'|trans({}, 'FOSUserBundle') }}</a>
        {% endif %}
        </div>
    </form>
</div>
